services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cloak
      POSTGRES_PASSWORD: cloakpass
      POSTGRES_DB: cloakapi
    ports:
      - "5433:5432"
    volumes:
      - cloakapi_pgdata:/var/lib/postgresql/data

  api:
    build:
      context: ../backend
      dockerfile: src/CloakAPI.Api/Dockerfile
    environment:
      ASPNETCORE_URLS: http://+:8081
      ConnectionStrings__Default: Host=postgres;Port=5432;Database=cloakapi;Username=cloak;Password=cloakpass
      Jwt__Issuer: CloakAPI
      Jwt__Audience: CloakAPI.Clients
      Jwt__Key: CloakAPI__DEV__SUPER_LONG_SECRET_KEY_32CHARS_MINIMUM!!!
    depends_on:
      - postgres
    ports:
      - "8081:8081"

  guardrail:
    build:
      context: ../backend
      dockerfile: src/CloakAPI.Guardrail/Dockerfile
    environment:
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Default: Host=postgres;Port=5432;Database=cloakapi;Username=cloak;Password=cloakpass
      Jwt__Issuer: CloakAPI
      Jwt__Audience: CloakAPI.Clients
      Jwt__Key: CloakAPI__DEV__SUPER_LONG_SECRET_KEY_32CHARS_MINIMUM!!!
    depends_on:
      - postgres
      - api
    ports:
      - "8080:8080"

  dashboard:
    build:
      context: ../frontend/cloakapi-dashboard
      dockerfile: Dockerfile
    environment:
      NEXT_PUBLIC_GUARDRAIL_BASE_URL: http://localhost:8080
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8081
    depends_on:
      - guardrail
    ports:
      - "3000:3000"

volumes:
  cloakapi_pgdata: